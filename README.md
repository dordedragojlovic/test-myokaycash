# Okay Demo (MyOkayCash Ebank)

## Setup

1. Clone repository
2. `yarn install`
3. Generate GraphQL schema types by `yarn gen:types`
4. `yarn dev`
5. Application is running on [localhost:3000](http://localhost:3000/)
